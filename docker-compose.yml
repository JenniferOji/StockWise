version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "4000:4080"  

  frontend:
    build: ./frontend
    ports:
      - "3000:80" 

  ml:
    build: ./ml
    ports:
      - "5000:5000"  